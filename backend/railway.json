{
  "build": {
    "builder": "nixpacks",
    "config": {
      "nodeVersion": "18",
      "installCommand": "npm install",
      "buildCommand": "npm run build",
      "startCommand": "npm start"
    }
  }
}